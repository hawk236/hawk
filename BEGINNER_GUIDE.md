# 🤍 完全新手指南

不用担心，按照下面的步骤一步步来就可以了！

## 第一步：打开命令行

### Windows 用户

1. 按键盘上的 **Win + R**（同时按住 Windows 键和 R 键）
2. 会弹出一个小窗口
3. 在窗口里输入：`cmd` 然后按 Enter
4. 黑色的命令行窗口就打开了

**长这样：**
```
C:\Users\你的用户名>
```

### Mac 用户

1. 按 **Command + Space**
2. 输入 `terminal` 回车
3. 黑色的终端窗口就打开了

## 第二步：进入项目文件夹

现在在命令行里输入下面的命令。

**假如你的项目文件夹在 D 盘：**
```
cd D:\hawk
```

**或者你的项目在其他位置，比如桌面：**
```
cd C:\Users\你的用户名\Desktop\hawk
```

**或者你的项目在文档里：**
```
cd C:\Users\你的用户名\Documents\hawk
```

> 💡 **提示**：输入完了记得按 Enter 键！

## 第三步：安装依赖（只需一次）

在命令行里输入：

```
npm install
```

然后按 Enter，等待······

屏幕会显示一堆文字，这是在下载东西。**完全正常！** ⏳

可能需要 1-5 分钟，取决于你的网速。

**完成的标志：** 屏幕的最后会显示：
```
added XXX packages
```

## 第四步：运行宠物

命令行里输入：

```
npm start
```

然后按 Enter！

**稍等几秒钟...**

🎉 **你的宠物窗口就出现了！**

一个可爱的白色小团子会在屏幕上显示！

---

## 完整的操作示例

看起来是这样的：

```
C:\Users\张三> cd D:\hawk
D:\hawk> npm install
（等待中... 会显示很多文字...）
added 145 packages

D:\hawk> npm start
（再等待几秒...）

✨ 宠物出现了！✨
```

---

## 🎮 开始玩耍

宠物出现后，你可以：

| 操作 | 效果 |
|------|------|
| **单击宠物** | 宠物会做出反应（开心、害羞或惊吓） |
| **拖动宠物** | 把宠物窗口拖到其他地方 |
| **双击宠物** | 宠物会开始看书学习 |
| **不理宠物** | 宠物会自己做事（睡觉、哭泣等） |

---

## ❌ 常见问题

### Q: 我输入了 `npm install` 但什么都没有发生？

**A:** 这很可能是网络问题。试试：
1. 检查是否连接网络
2. 重新输入命令
3. 等待更久（可能的话给 10 分钟）

如果还是不行，检查是否正确安装了 Node.js（见下面）

### Q: 我输入了 `npm install` 但显示错误信息？

**A:** 可能是没有安装 Node.js

**解决办法：**
1. 访问 https://nodejs.org/
2. 下载 **LTS 版本**（绿色的那个）
3. 双击安装，一路点 "Next" 和 "Install"
4. 安装完后，重新打开命令行
5. 输入 `node --version` 看是否成功（应该显示版本号）

### Q: 宠物窗口看不到？

**A:** 可能在屏幕的其他地方。检查：

1. **屏幕角落** - 看看左上角或右下角
2. **任务栏** - 看看任务栏中是否有一个 electron 的图标
3. **重新启动** - 关闭命令行，然后重新运行 `npm start`

### Q: 怎么停止宠物？

**A:** 很简单！点击宠物窗口的 X 按钮就可以关闭。

或者在命令行里按 **Ctrl + C**

---

## 🎨 修改宠物外观

如果你想改变宠物的颜色或大小：

1. 用记事本或 VS Code 打开 `src/index.html` 文件
2. 找到这一行：
   ```
   <circle id="body" cx="100" cy="130" r="60" fill="#f5f5f5"
   ```
3. 修改 `fill="#f5f5f5"` 的颜色值

**常见颜色代码：**
- `#ff0000` = 红色 ❤️
- `#00ff00` = 绿色 💚
- `#0000ff` = 蓝色 💙
- `#ffff00` = 黄色 💛
- `#ffffff` = 白色 ⚪

4. 保存文件
5. 重新启动 `npm start`
6. 宠物变色了！

---

## 📚 更多帮助

- **详细的新手问题** → 看 [QUICKSTART.md](QUICKSTART.md)
- **Windows 特殊设置** → 看 [WINDOWS_SETUP.md](WINDOWS_SETUP.md)
- **想修改更多东西** → 看 [SVG_DESIGN.md](SVG_DESIGN.md)

---

## ✅ 检查清单

你需要：

- ✅ Windows、Mac 或 Linux 电脑
- ✅ 命令行（我们已经打开了）
- ✅ Node.js（如果没有，上面有安装链接）
- ✅ 这个项目文件夹

有了这些，你就可以开始了！

---

## 🎯 总结（最简单的版本）

1️⃣ 打开命令行 (Win + R → 输入 cmd)

2️⃣ 进入文件夹 (输入 `cd D:\hawk` 或你的路径)

3️⃣ 安装东西 (输入 `npm install`)

4️⃣ 运行宠物 (输入 `npm start`)

5️⃣ 享受！

就这么简单！🎉

---

如果还有不懂的地方，**告诉我具体是哪一步出了问题**，我再帮你详细解释！😊
